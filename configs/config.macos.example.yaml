peer_id: macbook-sender

firebase:
  project_id: streamd-p2p-signaling
  credentials_file: ~/.streamd/service-account.json

audio:
  sample_rate: 48000
  channels: 1
  frame_ms: 20
  # Optional: exact device name from Audio MIDI Setup / malgo enumeration
  capture_device: BlackHole 2ch

webrtc:
  stun_servers:
    - stun:stun.l.google.com:19302

log_level: info
